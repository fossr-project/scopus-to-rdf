PREFIX pub: <https://w3id.org/fossr/onto/publications/>
PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX prism: <http://prismstandard.org/namespaces/basic/2.0/>

SELECT * WHERE {
    ?pub a pub:Publication.
    OPTIONAL {?pub dc:title ?title}
    OPTIONAL {?pub prism:coverDate ?date}
    OPTIONAL {
    	?pub pub:hasAuthorList/pub:contains [
        	pub:hasItemContent ?author;
            pub:index ?authorOrder
    	]
    }
    OPTIONAL {
    	?pub pub:hasReferenceList/pub:contains/pub:hasItemContent ?referenced_pub
    }
    OPTIONAL {
	    ?referencing_pub pub:hasReferenceList/pub:contains/pub:hasItemContent ?pub
    }
}
LIMIT 100